# Ferramentas offline (Python)

```bash
cd tools
python -m venv .venv
# Windows: .venv\Scripts\activate
pip install -e ".[dev]"
```

- **Retratos / ComfyUI:** `portrait-gen/README.md` — geração de `prompts_batch.json` e runner da API.
- **World Pack** (Python): em evolução na Phase 1 (`docs/implementation-plan.md`, P1-10+).
