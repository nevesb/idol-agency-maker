# Copie para .env e preencha (nunca commite .env)
PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Gemini API (LLM enrichment + image generation)
GEMINI_API_KEY=

# RunPod (só servidor — não usar prefixo PUBLIC_)
# Ver: src/lib/server/runpod-env.ts · scripts: npm run runpod:start / runpod:stop
RUN_POD_API_KEY=
RUNPOD_POD_ID=z7iqhn9lq3shkl

# ComfyUI na RunPod (geração local via Python)
# COMFYUI_HOST=https://SEU_POD-8188.proxy.runpod.net
